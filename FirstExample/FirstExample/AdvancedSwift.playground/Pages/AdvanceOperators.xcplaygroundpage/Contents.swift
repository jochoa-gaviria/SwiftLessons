import UIKit

//Associativity -- using brackets '()'
let myInt: Int = (((2 + 3) % 4) * 5)
let myInt2: Int = (2 + ((3 % 4) * 5))

//Advance operators
// bit to bit
// displacement operator
// overflow operator
// custom operators
